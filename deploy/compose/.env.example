# Copy to .env and adjust if the defaults collide with something already running.
# .env is gitignored. Nothing in this file is a secret; it is local-only
# configuration for the compose stack.

# Host port PostgreSQL is published on. Change this if 5432 is already taken.
OPSATLAS_DB_PORT=5432
