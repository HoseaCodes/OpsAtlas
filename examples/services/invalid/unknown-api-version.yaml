# An apiVersion the platform does not know is a validation error, never a guess
# at the nearest version (CLAUDE.md section 8).
apiVersion: opsatlas.ambitiousconcepts.io/v2
kind: Service
metadata:
  name: orders-api
  repository: ambitious-concepts/orders-api
spec:
  tier: 1
  environments:
    - name: production
