# "30 days" is not a window the error-budget calculation can parse.
apiVersion: opsatlas.ambitiousconcepts.io/v1
kind: Service
metadata:
  name: orders-api
  repository: ambitious-concepts/orders-api
spec:
  tier: 1
  environments:
    - name: production
  operations:
    slo:
      availability: 99.9
      window: 30 days
