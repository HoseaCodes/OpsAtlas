# ── Build output ─────────────────────────────────────────────────────────
build/
.gradle/
bin/
out/
target/
node_modules/
.next/
dist/
*.tsbuildinfo

# ── Secrets and local config (CLAUDE.md §3.5) ────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── Tooling ──────────────────────────────────────────────────────────────
.DS_Store
.idea/
*.iml
.vscode/
coverage/
test-results/
playwright-report/
blob-report/
.playwright/

# ── Superseded design reference ──────────────────────────────────────────
# The v3 HTML prototype is a local visual reference only. It is deliberately
# not committed: its seeded data uses insurance-domain service names, which
# docs/design/tokens.md §"Example data" rules out for this project. Every
# design decision worth keeping has been extracted into docs/design/tokens.md;
# read that instead. See docs/adr/0006-prototype-is-not-committed.md.
service-operations-platform-v3.html

# ── Playwright ───────────────────────────────────────────────────────────
apps/web/test-results/

# ── Observer ─────────────────────────────────────────────────────────────
apps/observer/observer
